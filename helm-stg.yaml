
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-stg

env:
  APP_ENV: stg

ingress:
  enabled: true
  hosts:
    - host: "viewem-stg.apps-test.iu.edu"
      paths: []

activeProfiles: "redis,stg"

serviceAccountName: viewem-stg

cluster: es-test