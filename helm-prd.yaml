
projectedSecret:
  - secret:
      name: "properties-2021-06-07"
  - secret:
      name: "lms-security-properties-20201014"

image:
  tag: stable

env:
  APP_ENV: prd

ingress:
  enabled: true
  hosts:
    - host: "viewem.apps.iu.edu"
      paths: []

activeProfiles: "redis,prd"

serviceAccountName: viewem-prd

cluster: es-prod