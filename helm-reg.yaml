
projectedSecret:
  - secret:
      name: "properties-2021-03-18"

image:
  tag: unstable-reg

env:
  APP_ENV: reg

ingress:
  enabled: true
  hosts:
    - host: "viewem-reg.apps-test.iu.edu"
      paths: []
